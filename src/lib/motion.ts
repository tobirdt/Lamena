/** Shared motion tokens — ease-out for entrances (Emil: UI under 300ms feels responsive). */
export const EASE_OUT = [0.23, 1, 0.32, 1] as const

export const REVEAL_VIEWPORT = { once: true, margin: '-72px' } as const
